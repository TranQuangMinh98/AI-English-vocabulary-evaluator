# Optional. Leave unset for same-origin deployments (Vercel) and local dev,
# where the Vite proxy / vercel.json routing handle /api automatically.
# Set this only if the API is hosted on a DIFFERENT origin than the client,
# e.g. VITE_API_URL=https://api.example.com
# VITE_API_URL=
